import styled from 'styled-components'

const Scroller = styled.div`
  position: absolute;
  top: 60px;
  bottom: 0px;
  overflow-x: hidden;
  overflow-y: scroll;
  width: 100%;
  z-index: 1;
  margin-bottom: 56px;
`

export default Scroller
